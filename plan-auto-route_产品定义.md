# plan-auto-route 产品定义

## 1. 文档信息

* 产品名称：plan-auto-route 城市无人机物流航线自动规划技能
* 版本号：当前代码版本以 `scripts/plan_auto_route.py` 中 `ALGORITHM_VERSION` 为准；当前标识为 `plan-auto-route-latest-air-corridor-v4-refactor-landuse-softnofly-astarfix-existing-route-3d-avoid`
* 所属系统 / 平台：Codex / OpenClaw 技能体系；可作为本地 CLI 工具独立运行
* 负责人：待定
* 参与人：航线规划算法开发、风险评估、测试验证、业务验收相关人员
* 创建 / 更新日期：`2026-04-24`
* 文档状态：`草案`
* 关联文档链接：
  * `SKILL.md`
  * `ALGORITHM.zh-CN.md`
  * `tests/test_line_risk_overlap.py`

---

## 2. 产品概述

### 2.1 产品定位（一句话）

plan-auto-route 是一个面向城市 UAV 物流任务的 OD 自动航线规划软件技能，用开放数据、禁飞约束、风险评分和三维高度规划，从起终点生成可审查、可导出的三维航线方案。

它不是单纯的 KML 后处理工具，而是从起终点坐标或 KML 端点出发，完成数据准备、候选选线、风险评估、高度剖面、KML/HTML/证据包输出的端到端规划工具。

### 2.2 目标用户与典型场景

* 目标用户角色：
  * 航线规划工程师
  * 城市低空物流运营人员
  * 风险评估 / 合规审查人员
  * 算法工程师与测试工程师
  * Codex / OpenClaw 自动化代理

* 主要使用场景：
  * 新建城市物流航线：输入起终点地址或坐标，自动生成安全优先和效率优先两类候选航线。
  * 禁飞与敏感区避让：结合民航机场禁飞区、军用/直升机场开放数据、学校幼儿园、关键基础设施、自定义 KML 禁飞区进行硬约束或软惩罚。
  * 三维高度规划：基于地形、建筑、障碍物和飞行包线生成绝对高程 KML 航点。
  * 既有航线避让：加载已有 KML 航线库，按水平和垂直缓冲进行 3D 硬避让。
  * 审查交付：输出 HTML 预览图、KML、候选摘要、Pareto 摘要、覆盖率、证据包和快照，支持人工复核。

### 2.3 核心价值与目标

* 业务目标：
  * 提效：减少从 OD 到初版航线的人工绘制和反复检查时间。
  * 风险控制：将人口、禁飞、学校、关键设施、线性风险、建筑障碍和高度约束纳入统一评估。
  * 可审计：输出结构化证据包和运行快照，便于复盘规划依据。
  * 复用：通过 bbox 缓存减少同城近邻任务的重复数据下载。

* 关键价值点：
  * 从单一 OD 输入自动生成至少 `safety_default` 与 `efficiency` 两类候选。
  * 输出同时包含可飞 KML、可视化 HTML 和机器可读 JSON 元数据。
  * 支持民航禁飞区本地数据、自定义禁飞 KML、既有航线 KML 的组合约束。
  * 支持线性风险缓冲重叠识别，避免航线与高速、高铁、高压线等长距离重叠。
  * 通过 `output/auto_route_bbox_cache` 复用同城缓存，降低 Overpass 等外部服务波动影响。

### 2.4 关键约束

* 法规 / 标准约束：
  * 当前工具提供规划与风险评估辅助，不替代正式空域审批、SORA/JARUS 或本地监管合规流程。
  * 民航机场禁飞区依赖本地 CAAC 转换数据，数据版本和几何质量需要单独管理。
  * 军事机场、直升机场、学校、关键设施等开放数据存在覆盖不完整风险，正式任务前需要人工复核。

* 技术环境约束：
  * 部署方式：本地 CLI / 技能目录部署；可被 Codex 或 OpenClaw 调用。
  * 支持环境：当前以 macOS 本地 Python 运行环境为主；需具备 `python3`。
  * 依赖的第三方服务 / 中间件：Overpass、Nominatim、OpenTopoData、WorldPop 下载源；本地依赖包括 Shapely、pyproj、GDAL、folium 等。
  * 当前仓库未提供标准化 `requirements.txt` 或 `pyproject.toml`，环境复现需要补充依赖清单。

* 性能约束：
  * 单次规划耗时受 bbox 大小、Overpass 响应、WorldPop/地形数据下载、建筑数据规模影响。
  * 当前没有稳定 SLA 或压测基线；应以实际城市、线路长度和缓存命中率做性能验收。
  * bbox 缓存可降低重复运行成本，但首次运行仍可能受外部 API 速度限制。

* 安全约束：
  * 输入 KML、输出 KML/HTML/JSON 均在本地文件系统处理，默认无用户认证和权限隔离能力。
  * 如集成到多人系统，需要补充访问控制、文件隔离、审计日志和敏感坐标脱敏策略。
  * 自定义禁飞区、既有航线库等文件必须使用可信来源。

* 其他重要前提假设：
  * 本产品面向“规划辅助”和“审查材料生成”，最终航线是否可执行仍需业务、飞控、监管和现场安全确认。
  * 开放数据质量直接影响规划质量，关键任务必须配合人工复核和实地/业务数据校验。

---

## 3. 需求概览

### 3.1 功能需求列表（精简版）

| 需求 ID | 功能点 | 典型使用场景 | 价值说明 | 优先级 (M/S/C/W) | 验收要点（怎么算完成） |
| --- | --- | --- | --- | --- | --- |
| FR-01 | OD 坐标规划 | 用户提供起终点经纬度 | 快速生成航线 | M | 支持 `--start-lon --start-lat --end-lon --end-lat` 并输出 KML/HTML/meta |
| FR-02 | 地址确认入口 | 用户提供起终点地址 | 降低坐标输入门槛 | M | `plan_auto_route_confirm.py` 可解析地址、展示候选、确认后调用主规划器 |
| FR-03 | OD KML 端点规划 | 用户已有一条参考 KML | 从既有文件提取起终点 | S | 支持 `--od-kml`，使用首末点作为 OD |
| FR-04 | 安全优先候选 | 新航线规划 | 输出保守风险偏好的候选 | M | 生成 `safety_default` 候选及对应 KML/摘要 |
| FR-05 | 效率优先候选 | 对比路径效率 | 输出更偏距离/效率的候选 | M | 生成 `efficiency` 候选及对应 KML/摘要 |
| FR-06 | 开放数据禁飞避让 | 城市航线避让机场/军用/直升机场 | 防止进入硬禁飞区 | M | 默认启用 `--open-data-no-fly`，起终点在硬禁飞区内时阻断 |
| FR-07 | 自定义禁飞区 | 用户有本地 KML 禁飞边界 | 支持业务自定义管控区 | M | 支持重复传入 `--custom-no-fly-kml` 并纳入硬约束 |
| FR-08 | 学校幼儿园硬避让 | 城市敏感区域规划 | 控制人群敏感点风险 | M | 学校/幼儿园进入硬缓冲约束，大学不按学校敏感硬约束处理 |
| FR-09 | 关键基础设施风险 | 避免靠近水厂、电站、油罐等 | 降低设施风险 | M | OSM P1/P2 类设施进入风险评分或硬缓冲 |
| FR-10 | 线性风险惩罚 | 避免长距离贴近高速/高铁/高压线 | 减少线性设施叠覆 | M | 计算中心线和缓冲区重叠信号，并进入评分路径 |
| FR-11 | 既有航线 3D 避让 | 同区域已有航线库 | 降低航线冲突 | S | 支持 `--existing-routes-dir`，按水平/垂直缓冲做 3D 避让 |
| FR-12 | 参考航线最小改动重规划 | 需要在既有航线基础上微调 | 控制偏移和绕行 | S | 支持 `--reference-kml`，输出偏移和绕行指标 |
| FR-13 | bbox 增量数据准备 | 同城多次规划 | 减少重复下载 | M | 生成 `output/auto_route_bbox_cache` 并记录复用统计 |
| FR-14 | 人口风险采样 | 低空穿越城市区域 | 控制人口暴露 | M | 读取/裁切 WorldPop，并输出路径人口统计 |
| FR-15 | 三维高度剖面 | 输出可飞高度航点 | 满足净空和飞行包线 | M | 输出绝对高程 KML，meta 中包含高度剖面指标 |
| FR-16 | 转角与航点后处理 | 减少不可飞急转和冗余航点 | 提升航线可执行性 | M | 支持最小转角、裁弯、航点预算控制 |
| FR-17 | HTML 可视化预览 | 人工审查航线 | 直观看风险层和候选 | M | 输出 `<name>.html`，支持安全/效率图层切换 |
| FR-18 | 证据包输出 | 审查和复盘 | 形成机器可读证明材料 | M | 输出 `<name>_evidence.json`，包含约束、目标、覆盖和选择路径 |
| FR-19 | 运行快照 | 复现实验和问题排查 | 追踪输入、算法版本和输出 | M | 输出 `snapshots/<name>_snapshot.json` |
| FR-20 | Pareto 策略 | 多目标选线 | 在距离、人口、垂向能耗间平衡 | S | 支持策略文件和业务线覆盖参数 |

### 3.2 关键性能与非功能指标

| 指标项 | 目标值 / 范围 | 说明（场景、条件） |
| --- | --- | --- |
| 规划成功率 | 目标不低于 95% | 在外部服务可用、OD 不在硬禁飞区、bbox 数据可获取的有效输入下统计 |
| 输出完整性 | 100% | 成功运行必须输出 KML、HTML、meta、candidates、coverage、evidence、snapshot |
| 硬约束阻断 | 100% | 起终点在硬禁飞区内必须报错阻断；航线图搜索不得穿越硬禁飞区 |
| 转角约束 | 满足配置阈值 | 默认最终航线最小内角不低于 `--min-turn-angle-deg` 约束 |
| 高度可行性 | 100% | 成功输出的航线必须满足净空、真高范围、硬高度上限和爬升/下降包线 |
| 可复现性 | 每次成功运行生成快照 | 快照记录算法版本、输入、候选、失败原因和输出路径 |
| 缓存复用可观测性 | 100% | 每次运行记录 bbox 缓存复用数量和增量查询 bbox |
| 依赖降级可解释性 | 必须有错误信息 | 外部服务失败、数据缺失、无可行路径时给出明确错误或 failure summary |

### 3.3 标准与合规要求（如适用）

* 外部标准：
  * 低空飞行监管、空域审批、城市运行安全要求需要按实际地区和任务另行确认。
  * SORA/JARUS 等风险评估流程可作为后续合规门禁，但当前产品不内置完整合规审批。

* 内部标准与规范：
  * 输出文件命名和目录需遵循 `output/auto_routes/<name>*` 约定。
  * 历史脚本保留在 `archive/`，生产流程只使用 `scripts/plan_auto_route.py`。
  * 所有算法变更应补充回归测试，尤其是风险评分、禁飞约束、高度规划和输出结构。

* 需要重点满足的合规要求简要说明：
  * 禁飞区数据版本可追溯。
  * 自定义禁飞和既有航线输入来源可追溯。
  * 规划结果需标注“辅助规划，不代表最终审批通过”。

---

## 4. 方案与系统边界

### 4.1 系统构成（高层）

* 产品形态：本地 CLI 工具 / Codex 技能 / OpenClaw 可调用技能

* 技术栈概要：
  * 前端技术：无独立前端应用；使用 folium 生成静态 HTML 预览。
  * 后端技术：Python 单体脚本。
  * 数据库：无数据库；使用本地文件、GeoJSON、KML、TIF、JSON 和 bbox 缓存。
  * 中间件/消息队列：无。

* 主要子系统 / 模块：
  * 交互确认模块：`scripts/plan_auto_route_confirm.py`，负责地址解析、用户确认和命令组装。
  * 数据准备模块：bbox 缓存、Overpass 查询、WorldPop 裁切、禁飞区和 OSM 图层加载。
  * 风险索引模块：人口、landuse、学校、关键设施、线性风险、建筑/障碍物索引。
  * 导航图与 A* 模块：road/water + oblique air lattice 构图，带风险成本和转角惩罚搜索。
  * 候选选择模块：安全优先、效率优先、Pareto 选择、垂向权衡和参考航线偏移控制。
  * 高度规划模块：地形、建筑、障碍、净空、爬升/下降包线和航点压缩。
  * 输出模块：KML、HTML、meta、candidates、pareto、coverage、evidence、snapshot。

### 4.2 系统边界与外部接口

* 上游 / 输入：
  * 用户输入：城市名、OD 坐标、起终点地址、OD KML、参考 KML、自定义禁飞 KML、已有航线 KML 目录、参数配置。
  * 本地数据：`config/civil_airport_no_fly.geojson`、`config/pareto_policies.json`、可选 DEM GeoTIFF。
  * 在线数据：Overpass、Nominatim、OpenTopoData、WorldPop。

* 下游 / 输出：
  * 航线规划人员：KML 航线、HTML 预览图。
  * 审查 / 风险评估人员：meta、coverage、evidence、snapshot。
  * 自动化流程：JSON 摘要和候选输出，可被后续 workflow 或报告生成流程消费。

* 关键接口：
  * CLI 接口：`python3 scripts/plan_auto_route.py ...`，主规划入口。
  * 确认 CLI 接口：`python3 scripts/plan_auto_route_confirm.py ...`，地址解析和用户确认入口。
  * 文件接口：KML / GeoJSON / JSON / HTML / TIF。
  * 第三方集成：Overpass API、Nominatim API、OpenTopoData API、WorldPop 数据下载源。

### 4.3 关键设计决策与约束（选填）

* 已经拍板的技术路线 / 架构模式：
  * 使用单体 Python 脚本承载主流程，降低部署和调用复杂度。
  * 使用本地 bbox 缓存优先、缺口增量查询的运行时数据准备策略。
  * 使用 road/water + oblique air lattice，避免航线被道路网络强绑定。
  * 默认输出双候选：`safety_default` 和 `efficiency`。
  * 证据包和快照默认开启，强化审计和可复现。

* 不能突破的限制：
  * 不能把历史 `archive/` 脚本作为生产入口。
  * 不能把规划结果直接等同于监管审批结果。
  * 不能在缺少关键开放数据或硬约束不可满足时强行输出“成功航线”。

* 技术债务处理策略：
  * 补充标准依赖声明和环境初始化说明。
  * 将超大主脚本逐步拆分为数据准备、风险评分、图搜索、高度规划、输出渲染等模块。
  * 增加端到端样例和小型离线 fixture，减少测试对外部网络依赖。
  * 对性能、缓存命中、外部 API 失败路径建立稳定回归测试。

---

## 5. 风险与未决事项

### 5.1 主要风险

| 编号 | 风险描述 | 影响 | 应对思路 | 状态（开放/已缓解） |
| --- | --- | --- | --- | --- |
| R-01 | 开放数据不完整或过期，导致禁飞区、设施、学校、建筑等识别遗漏 | 高 | 关键任务引入人工复核、本地权威数据覆盖、数据版本记录 | 开放 |
| R-02 | 外部 API 不稳定，导致首次规划失败或耗时过长 | 中 | bbox 缓存、失败重试、离线数据包、明确错误提示 | 部分缓解 |
| R-03 | 当前缺少标准依赖清单，新环境复现成本高 | 中 | 增加 `requirements.txt` / `pyproject.toml` 和环境验证脚本 | 开放 |
| R-04 | 主脚本体量过大，后续维护和局部测试成本高 | 中 | 模块化拆分，保留现有 CLI 兼容层 | 开放 |
| R-05 | 高度规划依赖 DEM / OpenTopoData 与 OSM 建筑数据质量 | 高 | 支持高质量本地 DEM，输出质量评估，关键区域人工检查 | 开放 |
| R-06 | 规划结果被误用为正式可飞许可 | 高 | 文档、输出和 UI 中明确辅助规划属性，增加合规审批门禁说明 | 开放 |
| R-07 | HTML 输出复杂，前端交互回归难以自动验证 | 中 | 增加浏览器快照测试和关键图层检查 | 开放 |

### 5.2 未决事项 / 待确认问题

* 待明确的业务口径：
  * 目标航线类型：医疗、商超、电商、巡检或其他业务线。
  * 默认偏好：安全优先、效率优先，还是按业务线启用 Pareto 策略。
  * “可接受绕行比例”“可接受平均偏移”“线性风险长距离重叠”的正式阈值。

* 需管理层或外部确认的决策点：
  * 是否作为正式生产工具接入低空运营流程。
  * 是否引入权威空域、禁飞区、地形和建筑数据。
  * 是否需要合规审查签核流程。

* 依赖其他项目 / 团队的事项：
  * 飞控 / 运营团队确认 KML 航点格式和高度策略是否满足实际机型。
  * 合规团队确认审批材料和审计字段要求。
  * 数据团队提供权威数据源或缓存发布机制。

* 技术选型待确认事项：
  * 是否继续保持单体技能，还是拆分为可复用 Python 包。
  * 是否提供 Web/API 服务化部署。
  * 是否建立离线数据包，减少外部服务实时依赖。

---

## 6. 版本规划与里程碑

| 阶段 / 版本 | 时间（预计） | 目标与主要交付物 |
| --- | --- | --- |
| MVP 最小可行版 | 已完成 | OD 到 KML/HTML 的主流程跑通，具备开放数据风险约束和基础高度规划 |
| V1 正式版 | 待定 | 补齐依赖清单、样例数据、端到端测试、使用手册和验收样例 |
| V2 增强版 | 待定 | 服务化接口、离线数据包、更多城市数据质量评估、浏览器自动化验证 |
| V3 成熟版 | 待定 | 生产级监控、权限隔离、审计台账、合规审批衔接和多团队运维流程 |

---

## 7. 附加信息（选填）

### 7.1 成本与商业要点（选填）

* 开发与运维成本估算：
  * 当前以本地技能方式运行，主要成本来自算法维护、数据源维护、测试验证和人工复核。
  * 若服务化部署，需要增加计算资源、对象存储、任务队列、监控告警和权限系统成本。

* 主要成本驱动因素：
  * 城市级开放数据下载、清洗和缓存维护。
  * DEM、禁飞区、建筑物、人口等高质量数据源采购或整理。
  * 算法回归测试、HTML/KML 兼容性测试和实际航线验收。
  * 与飞控、审批、运营系统的集成成本。

* 预期定价策略：
  * 内部工具：按项目或团队内部成本分摊。
  * 若产品化：可按城市数据包、规划次数、企业授权或私有化部署收费。

* 用户规模预期：
  * 当前定位为专家工具和自动化技能，优先服务少量航线规划 / 风险评估人员。
  * 服务化后可扩展到运营人员和业务系统自动调用。

### 7.2 测试与验收要点（选填）

* 关键场景验收清单：
  * 坐标输入规划成功，输出完整文件集。
  * 地址确认入口可解析、确认并调用主规划器。
  * 自定义禁飞 KML 生效，航线不穿越硬禁飞区。
  * 已有航线目录加载成功，3D 避让约束生效。
  * 安全优先和效率优先候选均可输出并在 HTML 中切换。
  * 高度剖面满足净空、真高和爬升/下降约束。
  * evidence 和 snapshot 包含算法版本、输入、约束、候选、输出路径。
  * 线性风险缓冲重叠测试通过。

* 需要重点覆盖的边界 / 异常场景：
  * 起点或终点位于硬禁飞区。
  * Overpass / Nominatim / OpenTopoData 超时或返回空数据。
  * 自定义 KML 文件不存在、格式错误或坐标为空。
  * 已有航线目录不存在、没有 KML 或 KML 高度缺失。
  * 城市 bbox 过大导致数据量过载。
  * 无可行路径、转角约束不可满足、高度包线不可满足。
  * WorldPop 或 DEM 数据缺失。

* 验收通过标准：
  * 现有单元测试通过：`python3 -m unittest tests/test_line_risk_overlap.py`。
  * 至少一个代表性城市 OD 样例端到端生成完整输出。
  * 失败路径有明确错误信息或 candidate failure 记录。
  * 输出 HTML 可正常打开，KML 可被目标查看 / 飞控工具识别。

### 7.3 部署与运维（选填）

* 部署架构概要：
  * 当前：本地技能目录 + Python CLI 运行。
  * 推荐：固定 Python 环境、依赖锁定、数据缓存目录、输出目录和日志目录。
  * 未来：可封装为 API 服务或批处理任务服务。

* 监控与告警策略：
  * 本地模式：记录运行日志、失败原因、输出文件和 snapshot。
  * 服务化模式：监控任务成功率、规划耗时、外部 API 错误率、缓存命中率、输出完整性。

* 备份与灾备方案：
  * 备份 `config/` 权威配置、禁飞区数据、Pareto 策略文件和代表性验收样例。
  * 对 `output/auto_route_bbox_cache` 可按城市和日期做缓存归档。
  * 对正式交付航线的 KML、HTML、evidence、snapshot 做版本化保存。

* 发布策略：
  * 当前本地技能发布：通过 Git 分支、测试和人工验收后同步到目标技能目录。
  * 服务化后建议使用灰度发布；先对离线样例和少量内部任务验证，再扩大使用范围。
