# Plan Auto Route Archive

This folder stores historical snapshots and legacy documents for reference only.

Rules:
- Do not import or execute archived scripts in production workflow.
- The only runnable planner entry is `scripts/plan_auto_route.py`.
- Keep archived files read-only unless you are explicitly doing historical comparison work.
approval test
